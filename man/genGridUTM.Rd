% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genGridUTM.R
\name{genGridUTM}
\alias{genGridUTM}
\title{Generate Grid (UTM version)}
\usage{
genGridUTM(tilesize, extent, res, overlap = NULL, polygon = FALSE,
  prj = NULL)
}
\arguments{
\item{tilesize}{Numeric. Size (in # pixels) of the square tiles}

\item{extent}{Extent. Object of type \code{extent} from the target raster}

\item{res}{Numeric. Resolution of the target raster}

\item{overlap}{Numeric. Optional: size of overlap region (in # of pixels)}

\item{polygon}{Logical. Output a SpatialPolygons object as well?}

\item{prj}{Character. CRS projection string if \code{polygon} is \code{TRUE}}
}
\value{
\code{data.frame} with extent parameters (xmin, xmax, ymin, ymax) for each tile per row or a \code{list} containing a \code{data.frame} and a \code{SpatialPolygons} object if \code{polygon} is \code{TRUE}
}
\description{
Generate grid from raster data in a UTM projection
}
\examples{
\dontrun{
library(bfastSpatial)
data(tura)

tiles <- genGridUTM(50, extent(tura), 30)
print(tiles)

tiles <- genGridUTM(50, extent(tura), 30, polygon = TRUE, prj = projection(tura))
print(tiles)
plot(tura, 2)
plot(tiles$poly, add = TRUE)

tiles <- genGridUTM(50, extent(tura), 30, overlap = 5, polygon = TRUE, prj = projection(tura))
plot(tura, 2)
plot(tiles$poly, add = TRUE)
}
}
\author{
Ben DeVries
}

